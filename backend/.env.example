# Configurações do Backend Joalin FlowPlayer
# Copie este ficheiro para .env e altere os valores

# Chave secreta para JWT (MUDE ISTO EM PRODUÇÃO!)
SECRET_KEY=troca_esta_chave_por_uma_muito_forte_e_aleatoria

# Algoritmo de encriptação JWT
ALGORITHM=HS256

# Tempo de expiração do token em minutos
ACCESS_TOKEN_EXPIRE_MINUTES=60

# CORS - Origens permitidas (separadas por vírgula)
# Em desenvolvimento, use "*" ou especifique os IPs/portas
ALLOWED_ORIGINS=*

# Exemplo para produção:
# ALLOWED_ORIGINS=https://seuapp.com,https://app.seuapp.com

# Banco de dados (SQLite por padrão)
DATABASE_URL=sqlite:///./app.db

# Ambiente (development, production)
ENVIRONMENT=development

# Host e porta do servidor
HOST=0.0.0.0
PORT=8000
